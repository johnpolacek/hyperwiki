---
title: "Plan Generation Component Guidance"
description: "hyperwiki plan page."
wikiKind: "plan"
---

<PlanHero>
  <h1>Plan Generation Component Guidance</h1>
  <p>Make hyperwiki's plan creation and import-plan compiler surfaces explicitly use the new shadcn-backed MDX plan component vocabulary instead of falling back to generic sections and visible source dumps.</p>
</PlanHero>

<PlanSummary>
  <ul>
    <li>Status: complete</li>
    <li>Shape: focused generation-contract update</li>
    <li>Current unit: all units complete</li>
    <li>Next action: dogfood a new imported-project plan and confirm the visible unit pages use component cards rather than source dumps.</li>
    <li>Validation: <code>pnpm run check</code>, <code>cargo test --manifest-path src-tauri/Cargo.toml import_onboarding_runtime</code>, and <code>pnpm run build:frontend</code>.</li>
  </ul>
</PlanSummary>

<Decision title="Component availability must be part of the authoring contract">
  <p>Rendering support alone is insufficient. Any agent or runtime that creates plan MDX must be told which components exist and which content belongs in visible app UI versus agent-only Markdown context.</p>
</Decision>

<Steps>
  <Step title="Unit 01 - Prompt and compiler component guidance">
    <p>Update the regular plan creation prompt, import plan artifact repair prompt, bundled hyperwiki skill references, and deterministic import compiler templates.</p>
    <p><strong>Verification:</strong> Static smoke coverage proves the guidance remains present, and Rust compiler tests prove generated artifacts still validate.</p>
  </Step>
</Steps>

<Verification>
  <ul>
    <li>Ran <code>pnpm run check</code>.</li>
    <li>Ran <code>cargo test --manifest-path src-tauri/Cargo.toml import_onboarding_runtime</code>.</li>
    <li>Ran <code>pnpm run build:frontend</code>.</li>
  </ul>
</Verification>
